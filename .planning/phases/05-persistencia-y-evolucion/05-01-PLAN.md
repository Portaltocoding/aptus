---
phase: 05-persistencia-y-evolucion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/evolution.ts
  - src/content/history.ts
  - src/cli/render.ts
  - src/cli/commands/start.ts
  - src/cli/commands/history.ts
  - src/index.ts
  - .gitignore
  - src/core/evolution.test.ts
  - src/content/history.test.ts
  - src/cli/render.test.ts
autonomous: true
requirements: [PERS-01, PERS-02]
must_haves:
  truths:
    - "Cada sesión completada se guarda localmente sin intervención manual (PERS-01)."
    - "Carlos puede ver la evolución de sus scores por dimensión y readiness por rol entre sesiones sucesivas, tanto al terminar una sesión como con un subcomando de consulta (PERS-02)."
    - "El motor de evolución es puro (sin I/O, sin reloj); la marca de tiempo se inyecta desde la capa de I/O."
    - "Un historial corrupto falla rápido y claro; uno inexistente arranca vacío sin romper la primera sesión."
  prohibitions:
    - "El núcleo no lee el reloj ni el disco; toda I/O vive en content/cli."
    - "El historial personal no se versiona (data/ en .gitignore)."
  artifacts:
    - path: "src/core/evolution.ts"
      provides: "SessionRecord + buildSessionRecord + evolution (puro)"
      exports: ["SessionRecord", "buildSessionRecord", "evolution", "EvolutionReport"]
    - path: "src/content/history.ts"
      provides: "loadHistory/saveHistory (JSON, zod fail-fast)"
      exports: ["loadHistory", "saveHistory"]
    - path: "src/cli/commands/history.ts"
      provides: "subcomando history"
      exports: ["historyCommand"]
    - path: "src/cli/render.ts"
      provides: "renderEvolution"
      exports: ["renderEvolution"]
---

<objective>
Persistir cada sesión localmente y mostrar la evolución entre sesiones (por dimensión y por rol), sobre los resultados ya calculados. Cálculo en motor puro; I/O y validación en content; CLI compone y presenta, y añade un subcomando de consulta.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: núcleo + store — modelo de sesión, evolución y persistencia</name>
  <files>src/core/evolution.ts, src/content/history.ts, src/core/evolution.test.ts, src/content/history.test.ts</files>
  <action>
    - evolution.ts: SessionRecord; buildSessionRecord(timestamp, score, readiness); evolution(history) (última vs anterior; deltas por dimensión; cambios de nivel por rol).
    - history.ts: loadHistory (inexistente→[], corrupto→Error), saveHistory (crea dir, sobrescribe), con zod.
    - Tests: buildSessionRecord; evolution 0/1/2+ sesiones y solo-últimas-dos; store round-trip, dir, inexistente, corrupto, schema inválido.
  </action>
  <verify>
    <automated>npx vitest run src/core/evolution.test.ts src/content/history.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>test "$(grep -riE 'Math\.random|Date\.now|new Date' src/core/evolution.ts | wc -l)" -eq 0</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: CLI — guardar + evolución en start, subcomando history, render</name>
  <files>src/cli/render.ts, src/cli/commands/start.ts, src/cli/commands/history.ts, src/index.ts, .gitignore, src/cli/render.test.ts</files>
  <action>
    - renderEvolution (tabla ahora/antes/Δ + cambios de nivel por rol).
    - start.ts: carga historial (fail-fast) antes de la sesión; al terminar guarda y muestra evolución.
    - history.ts (comando) + subcomando 'history' en index.ts.
    - .gitignore: data/.
  </action>
  <verify>
    <automated>npx vitest run</automated>
    <human-check>Sesión real (PTY): 1ª sesión guarda y avisa; 2ª muestra evolución; `aptus history` lista el historial.</human-check>
  </verify>
</task>
</tasks>

<success_criteria>
Cada sesión se guarda sin intervención y Carlos ve su evolución por dimensión y por rol entre sesiones (al terminar y vía `history`). Verificado con tsc + vitest + UAT sobre PTY.
</success_criteria>

<output>Crear 05-01-SUMMARY.md al terminar.</output>
