---
phase: 03-confianza-y-calibracion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/scoring.ts
  - src/core/calibration.ts
  - src/core/session.ts
  - src/cli/runner.ts
  - src/cli/render.ts
  - src/cli/commands/start.ts
  - src/core/calibration.test.ts
  - src/core/session.test.ts
  - src/cli/render.test.ts
  - src/cli/start.integration.test.ts
autonomous: true
requirements: [SESS-03, RES-04]
must_haves:
  truths:
    - "Para cada pregunta, tras elegir respuesta se captura la confianza (baja/media/alta) con un 2º select, sin romper el flujo navegable de la sesión (SESS-03)."
    - "El resultado final incluye una tabla confianza-vs-acierto: por nivel, el acierto real con su N y una lectura de sobre/infra-estimación (RES-04)."
    - "El motor de calibración es puro (sin I/O, determinista) y no produce ningún score único agregado."
  prohibitions:
    - "El núcleo no adquiere I/O ni conocimiento del dominio; la seed y los prompts siguen viviendo solo en la capa CLI."
    - "La calibración no se muestra como un score agregado de 'empleabilidad'; es un diagnóstico por nivel de confianza."
  artifacts:
    - path: "src/core/calibration.ts"
      provides: "calibration(): CalibrationResult por nivel (declared vs accuracy, gap), puro"
      exports: ["calibration", "CalibrationResult", "ConfidenceBucket", "DECLARED_ACCURACY", "CONFIDENCE_ORDER", "CALIBRATION_GAP_THRESHOLD"]
    - path: "src/core/session.ts"
      provides: "confidences map + setConfidenceCurrent + toAnswered con confidence"
    - path: "src/cli/runner.ts"
      provides: "2º select de confianza por pregunta"
    - path: "src/cli/render.ts"
      provides: "renderCalibration(): tabla confianza-vs-acierto con lectura"
      exports: ["renderCalibration"]
---

<objective>
Capturar la confianza declarada por pregunta (SESS-03) y mostrar la curva confianza-vs-acierto en el resultado (RES-04), reutilizando la sesión y el resultado de Phase 1/2. El cálculo va en un motor puro nuevo; la CLI solo captura y presenta.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: núcleo — modelo de confianza + motor de calibración puro</name>
  <files>src/core/scoring.ts, src/core/calibration.ts, src/core/session.ts, src/core/calibration.test.ts, src/core/session.test.ts</files>
  <action>
    - `scoring.ts`: tipo Confidence (baja/media/alta) + campo opcional `confidence` en AnsweredQuestion.
    - `calibration.ts`: calibration(answered, bank) → buckets por nivel (declared representativo, accuracy real, gap), orden alta→baja, sin agregado.
    - `session.ts`: mapa `confidences`, `setConfidenceCurrent`, `toAnswered` propaga confidence.
    - Tests: buckets/accuracy, sobreestimación (gap<0), exclusión de sin-confianza/sin-responder, orden, plumbing de sesión.
  </action>
  <verify>
    <automated>npx vitest run src/core/calibration.test.ts src/core/session.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>test "$(grep -riE 'Math\.random|Date\.now' src/core/calibration.ts | wc -l)" -eq 0</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: CLI — captura de confianza + render de la curva</name>
  <files>src/cli/runner.ts, src/cli/render.ts, src/cli/commands/start.ts, src/cli/render.test.ts, src/cli/start.integration.test.ts</files>
  <action>
    - `runner.ts`: 2º select de confianza tras la respuesta (default a la previa).
    - `render.ts`: renderCalibration() con lectura sobre/infra/calibrado; mensaje claro si no hubo confianza.
    - `start.ts`: compone calibration + renderCalibration bajo la tabla por dimensión.
    - Tests de render y smoke end-to-end con confianza.
  </action>
  <verify>
    <automated>npx vitest run</automated>
    <human-check>Sesión real (PTY): responder con confianza y ver la tabla confianza-vs-acierto con la lectura de sobreestimación.</human-check>
  </verify>
</task>
</tasks>

<success_criteria>
Confianza capturada por pregunta sin romper el flujo, y curva confianza-vs-acierto en el resultado que revela dónde Carlos se sobreestima. Verificado con tsc + vitest + UAT sobre PTY.
</success_criteria>

<output>Crear 03-01-SUMMARY.md al terminar.</output>
