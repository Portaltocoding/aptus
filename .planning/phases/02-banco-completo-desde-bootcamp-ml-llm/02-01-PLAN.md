---
phase: 02-banco-completo-desde-bootcamp-ml-llm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packs/ai-ml-readiness/pack.yaml
  - packs/ai-ml-readiness/questions.yaml
  - packs/ai-ml-readiness/pack.test.ts
  - src/cli/commands/start.ts
autonomous: true
requirements: [CONT-04, SESS-04]
must_haves:
  truths:
    - "El pack real cubre las 5 dimensiones objetivo (llm-rag-evals, ml-clasico, fullstack-next-nest-ts, ai-product-system-design, comportamental-star) con preguntas reales y técnicamente correctas."
    - "Cada pregunta registra su fuente (bootcamp-ml-llm | externa) en la metadata, permitiendo auditar el sesgo de autocuración."
    - "El banco es sustancialmente mayor que lo mostrado por sesión (66 vs 25) y dos intentos con seeds distintas no presentan el mismo set (rotación, SESS-04)."
    - "Una sesión real cubre las 5 dimensiones con selección equilibrada y resultado por dimensión con N."
  prohibitions:
    - "El núcleo (src/core) y el loader NO adquieren conocimiento del dominio: las 5 dimensiones y su terminología viven solo en packs/ (ENG-04 intacto)."
    - "No se introduce persistencia ni readiness por rol (fuera de alcance de Phase 2)."
  artifacts:
    - path: "packs/ai-ml-readiness/pack.yaml"
      provides: "Metadata del pack v2: 5 dimensiones declaradas"
    - path: "packs/ai-ml-readiness/questions.yaml"
      provides: "66 preguntas hand-curadas con source auditable"
    - path: "packs/ai-ml-readiness/pack.test.ts"
      provides: "Tests: 5 dimensiones, >=12/dim, >=60 total, source auditable, mezcla de fuentes, rotación (SESS-04), determinismo"
    - path: "src/cli/commands/start.ts"
      provides: "Dimensionado a 25 preguntas (5/dimensión) para 5 dimensiones"
---

<objective>
Crecer el banco real de 2 a 5 dimensiones, curado desde el temario de `bootcamp-ml-llm` (inspección real de `module_maps.json`) más fuentes externas para lo no cubierto (fullstack), con procedencia por pregunta auditable (CONT-04) y rotación entre intentos (SESS-04). Sin tocar el núcleo puro: solo contenido en `packs/` y el dimensionado de la sesión.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 3 dimensiones nuevas + retag de procedencia + pack v2</name>
  <files>packs/ai-ml-readiness/pack.yaml, packs/ai-ml-readiness/questions.yaml, src/cli/commands/start.ts</files>
  <action>
    - Añadir 36 preguntas (12 por dimensión) para fullstack-next-nest-ts (externa), ai-product-system-design (mixta) y comportamental-star (externa).
    - Retag de las 30 existentes: source "curación manual" → "bootcamp-ml-llm" (temario m0-m8 del bootcamp).
    - pack.yaml: 5 dimensiones, version 2.0.0, semántica de source documentada.
    - start.ts: SESSION_TARGET_QUESTIONS=25, MIN_PER_DIMENSION=4.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>Sesión real (PTY) que cubre las 5 dimensiones con resultado por dimensión y N.</human-check>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests de auditoría de fuentes y rotación</name>
  <files>packs/ai-ml-readiness/pack.test.ts</files>
  <action>
    Reescribir pack.test.ts: 5 dimensiones exactas, >=12/dimensión, >=60 total, correct válido, explanation real, source auditable (bootcamp-ml-llm|externa), mezcla de fuentes presente (fullstack 100% externa), rotación con seeds distintas (SESS-04) y determinismo con misma seed.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
</task>

</tasks>

<success_criteria>
El banco completo cubre las 5 dimensiones con procedencia auditable y rota entre intentos, sobre la tubería de Phase 1 sin romperla. Verificado con tsc + vitest + UAT sobre PTY.
</success_criteria>

<output>
Crear 02-01-SUMMARY.md al terminar.
</output>
