---
phase: 06-integracion-jobhunt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packs/ai-ml-readiness/readiness.yaml
  - src/content/readiness.ts
  - src/content/jobhunt.ts
  - src/core/market.ts
  - src/cli/render.ts
  - src/cli/commands/start.ts
  - src/core/market.test.ts
  - src/content/jobhunt.test.ts
  - src/cli/render.test.ts
  - src/content/readiness.test.ts
autonomous: true
requirements: [INTEG-01]
must_haves:
  truths:
    - "Si jobs.db existe, los gaps prioritarios reflejan qué dimensiones pide más el mercado real de jobhunt (criterio 1)."
    - "Si jobs.db no existe o no es accesible, Aptus degrada con gracia: sesión y resultado funcionan igual, sin error ni bloqueo, sin ponderación (criterio 2)."
    - "El cruce nunca genera un score nuevo de encaje/empleabilidad: solo reordena/pondera los gaps ya calculados, con las señales a la vista (criterio 3)."
  prohibitions:
    - "El adaptador es solo lectura: nunca escribe en jobhunt ni lanza; el motor de mercado es puro."
    - "No se crea un score agregado; priority es solo clave de orden interna."
  artifacts:
    - path: "src/content/jobhunt.ts"
      provides: "loadJobTexts (solo lectura, null si no disponible)"
      exports: ["loadJobTexts"]
    - path: "src/core/market.ts"
      provides: "computeDemand + applyMarketWeight (puro)"
      exports: ["computeDemand", "applyMarketWeight", "MarketDemand", "WeightedGap"]
    - path: "src/cli/render.ts"
      provides: "renderWeightedGaps"
      exports: ["renderWeightedGaps"]
    - path: "packs/ai-ml-readiness/readiness.yaml"
      provides: "market_keywords por dimensión (opcional)"
---

<objective>
Ponderar la prioridad de los gaps por demanda de mercado leyendo jobhunt en solo lectura, con degradación elegante y sin crear scores nuevos. Adaptador de I/O aparte, motor de mercado puro, keywords como dato del pack.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: adaptador jobhunt + motor de mercado</name>
  <files>src/content/jobhunt.ts, src/core/market.ts, src/content/jobhunt.test.ts, src/core/market.test.ts, packs/ai-ml-readiness/readiness.yaml, src/content/readiness.ts</files>
  <action>
    - jobhunt.ts: loadJobTexts(dbPath) con node:sqlite readOnly; null si no existe/ilegible.
    - market.ts: computeDemand(jobTexts, keywords) y applyMarketWeight(gaps, demand) (puro).
    - readiness.yaml: market_keywords por dimensión; schema opcional.
    - Tests: computeDemand (conteo, case-insensitive, 1/oferta), applyMarketWeight (orden por debilidad×demanda), adaptador (db válida, ausente→null, corrupta→null).
  </action>
  <verify>
    <automated>npx vitest run src/core/market.test.ts src/content/jobhunt.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>test "$(grep -riE 'Math\.random|Date\.now|new Date' src/core/market.ts | wc -l)" -eq 0</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: render ponderado + composición con degradación elegante</name>
  <files>src/cli/render.ts, src/cli/commands/start.ts, src/cli/render.test.ts</files>
  <action>renderWeightedGaps (acierto + "en N/T ofertas" + plan); start.ts: loadJobTexts, si hay datos ponderar y renderizar, si no renderGaps plano.</action>
  <verify>
    <automated>npx vitest run</automated>
    <human-check>Sesión real (PTY) con jobs.db presente: gaps ponderados por demanda (275/403 ofertas para llm-rag-evals).</human-check>
  </verify>
</task>
</tasks>

<success_criteria>
Con jobhunt presente, los gaps se reordenan por demanda real de mercado; sin él, Aptus funciona igual. Nunca un score de encaje. Verificado con tsc + vitest + UAT sobre PTY.
</success_criteria>

<output>Crear 06-01-SUMMARY.md al terminar.</output>
