---
phase: 01-fundamento-end-to-end
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/cli/runner.ts
  - src/cli/render.ts
  - src/cli/render.test.ts
  - src/cli/commands/start.ts
  - src/cli/start.integration.test.ts
  - src/index.ts
autonomous: true
requirements: [SESS-01, SESS-02, RES-01]
must_haves:
  truths:
    - "`aptus start` (vía `tsx src/index.ts start`) carga el pack real, arranca una sesión select navegable y, al terminar, imprime un resultado por dimensión con el N junto a cada score (walking skeleton end-to-end)."
    - "La sesión es de opción múltiple (select), navegable: se puede volver atrás a una pregunta anterior y cambiar la respuesta antes de terminar (SESS-01)."
    - "La selección de preguntas de la sesión está equilibrada por dimensión con un mínimo por dimensión (SESS-02), usando `selectBalanced` del núcleo."
    - "El render muestra SIEMPRE `answered/presented` (el N) junto a cada porcentaje por dimensión (RES-01)."
    - "Ctrl+C durante la sesión sale limpio (captura `ExitPromptError`), sin corromper estado ni imprimir stack trace."
    - "Un pack inválido detiene el arranque con mensaje claro y código de salida ≠ 0 (no se inicia sesión con datos rotos)."
  prohibitions:
    - "El render (`src/cli/render.ts`) NO deriva, ajusta ni recalcula scores: solo formatea el `ScoreResult` ya calculado por el núcleo."
    - "La salida NUNCA muestra un score único agregado tipo 'empleabilidad'/'probabilidad de contratación': solo la matriz por dimensión con su N."
    - "El render NO muestra ningún porcentaje sin su N al lado."
  artifacts:
    - path: "src/cli/runner.ts"
      provides: "Bucle indexado @inquirer/prompts select con choice '◀ Volver' y salida limpia en Ctrl+C"
      exports: ["runSession"]
    - path: "src/cli/render.ts"
      provides: "renderResult(): tabla cli-table3 por dimensión con barras unicode picocolors y N junto al score"
      exports: ["renderResult"]
    - path: "src/cli/commands/start.ts"
      provides: "Composición del subcomando start: loadPack → selectBalanced → runSession → score → renderResult"
      exports: ["startCommand"]
    - path: "src/index.ts"
      provides: "Entrypoint commander con el subcomando start"
    - path: "src/cli/render.test.ts"
      provides: "Test de que cada fila del render lleva su N y no hay agregado"
    - path: "src/cli/start.integration.test.ts"
      provides: "Smoke no interactivo: pack fixture → selección con seed → score → render, con N presente"
  key_links:
    - from: "src/cli/commands/start.ts"
      to: "src/content/loader.ts, src/core/session.ts, src/core/scoring.ts, src/cli/render.ts"
      via: "composición end-to-end (cargar pack → seleccionar → puntuar → renderizar)"
      pattern: "loadPack|selectBalanced|score|renderResult"
    - from: "src/cli/render.ts"
      to: "src/core/scoring.ts"
      via: "consume el tipo ScoreResult; solo formatea, no calcula"
      pattern: "ScoreResult"
---

<objective>
La capa de terminal que cierra el walking skeleton: el runner interactivo `select` con navegación hacia atrás, el render del resultado por dimensión con N, el subcomando `start` que compone todo (cargar pack → seleccionar equilibrado → correr sesión → puntuar → renderizar), y el entrypoint commander. Tras este plan, Carlos puede ejecutar `aptus start` y completar una sesión real de punta a punta (SESS-01, SESS-02, RES-01).

Purpose: Conectar el núcleo puro (ya probado con fixtures) con la terminal, sin que la CLI derive ni ajuste nada: solo presenta y captura. La navegación hacia atrás NO es nativa de `@inquirer/prompts` (confirmado en issues del repo) — se implementa con el bucle indexado del research envolviendo la lógica de navegación pura del plan 03. El render mantiene el N siempre pegado al score.

Output: `src/cli/runner.ts`, `src/cli/render.ts` (+test), `src/cli/commands/start.ts`, `src/cli/start.integration.test.ts` y `src/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-fundamento-end-to-end/01-RESEARCH.md
@src/content/loader.ts
@src/core/scoring.ts
@src/core/session.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render del resultado (N siempre junto al score) + su test</name>
  <files>src/cli/render.ts, src/cli/render.test.ts</files>
  <read_first>
    @.planning/phases/01-fundamento-end-to-end/01-RESEARCH.md — Pattern 5 "Render de resultado" (líneas 458-487) y "Validation Architecture" RES-01 (línea 669).
    @src/core/scoring.ts — tipo ScoreResult.
  </read_first>
  <behavior>
    - Para un `ScoreResult` con 2 dimensiones, el string renderizado contiene una fila por dimensión, cada una con su porcentaje Y su `answered/presented` (N) al lado.
    - Ninguna fila muestra un porcentaje sin su N.
    - No aparece ninguna fila/celda de "total"/"agregado"/"empleabilidad".
    - Una dimensión con `answered === 0` se renderiza sin romper (0% con N 0/presented).
  </behavior>
  <action>
    Implementar `src/cli/render.ts` copiando el Pattern 5: `bar(pct, width)` con `█`/`░` coloreado por umbral con picocolors (verde/amarillo/rojo), y `renderResult(result: ScoreResult)` con `cli-table3` — una fila por dimensión con el `pct` y la columna `N (respondidas/presentadas)` siempre presente. Estética minimalista clásica (barras sobrias, sin arte). El render SOLO formatea; jamás deriva ni ajusta scores.
    - `render.test.ts`: afirmar que la salida de una fixture de `ScoreResult` contiene, por cada dimensión, tanto el `%` como su `N` (p.ej. que junto a cada `%` aparece un `/` de `answered/presented`), y que NO contiene ninguna etiqueta de agregado.
  </action>
  <verify>
    <automated>npx vitest run src/cli/render.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `renderResult` produce una tabla por dimensión con el N siempre junto al score y sin ninguna fila agregada; el test lo verifica sobre una fixture de ScoreResult.
  </acceptance_criteria>
  <done>renderResult muestra el N junto a cada score por dimensión, sin agregado; render.test.ts en verde.</done>
</task>

<task type="auto">
  <name>Task 2: Runner select navegable + subcomando start + entrypoint, con smoke end-to-end</name>
  <files>src/cli/runner.ts, src/cli/commands/start.ts, src/cli/start.integration.test.ts, src/index.ts</files>
  <read_first>
    @.planning/phases/01-fundamento-end-to-end/01-RESEARCH.md — Pattern 4 "Navegación hacia atrás" (líneas 400-450), Pitfall 1 (`default` es un value, líneas 509-513), "Code Examples" subcomando start con commander (líneas 590-606) y Open Question 2 (constantes de dimensionado, líneas 633-636).
    @src/core/session.ts — selectBalanced + estado de navegación pura.
    @src/content/loader.ts — loadPack.
  </read_first>
  <action>
    Implementar la composición de terminal:
    - `src/cli/runner.ts`: `runSession(questions)` con el bucle indexado del Pattern 4 usando `@inquirer/prompts` `select`. Choices = opciones de la pregunta + choice especial `◀ Volver` visible solo si `index > 0`. `default: <value>` (el id de la opción ya elegida) para reposicionar el cursor al revisitar (Pitfall 1: es un value, no un índice). Progreso "Pregunta N de M". Envolver la lógica de índice/registro delegando en las transiciones puras de `src/core/session.ts` (no reimplementar la máquina de estados aquí). Capturar `ExitPromptError` para salida limpia en Ctrl+C sin persistir nada. Devolver `AnsweredQuestion[]`.
    - `src/cli/commands/start.ts`: `startCommand()` que compone: `loadPack` del pack real por defecto (`packs/ai-ml-readiness/`, ruta resuelta de forma fija bajo `packs/` — no aceptar rutas arbitrarias del usuario en P1), `selectBalanced` con constantes de dimensionado (p.ej. SESSION_TARGET_QUESTIONS=20, MIN_PER_DIMENSION=8 — ajustables) y un shuffle con seed derivada del momento de arranque (la seed se genera en esta capa de IO, NUNCA dentro del núcleo), `runSession`, `score`, y `renderResult` a stdout. Si `loadPack` lanza, imprimir el mensaje claro y salir con código ≠ 0.
    - `src/index.ts`: entrypoint commander con `program.command("start")` que invoca `startCommand()` (copiar el Code Example del research). Requiere `"type": "module"` (ya presente, Pitfall 4).
    - `src/cli/start.integration.test.ts`: smoke NO interactivo que ejercita el camino end-to-end sin `@inquirer` — cargar el mini-pack fixture, `selectBalanced` con `makeSeededShuffle(seed)` fija, construir un `AnsweredQuestion[]` determinista, `score`, `renderResult`, y afirmar que el string final contiene el N junto a cada porcentaje y ninguna etiqueta agregada. Esto cubre la integración motor+contenido+render de forma determinista; la interactividad viva del select se verifica manualmente (abajo).
  </action>
  <verify>
    <automated>npx vitest run src/cli/start.integration.test.ts</automated>
    <automated>test "$(grep -riE 'overall|employab|hire.?prob' src/cli/render.ts src/cli/commands/start.ts | wc -l)" -eq 0</automated>
    <automated>test "$(grep -riE 'Math\.random|Date\.now' src/core/session.ts src/core/scoring.ts | wc -l)" -eq 0</automated>
    <automated>npx tsc --noEmit</automated>
    <human-check>Ejecutar `npm start` (o `tsx src/index.ts start`): completar una sesión sobre el pack real, volver atrás a una pregunta y cambiar la respuesta, confirmar el contador "Pregunta N de M", probar Ctrl+C (sale limpio) y revisar que el resultado final muestra el score por dimensión con su N y sin ningún agregado.</human-check>
  </verify>
  <acceptance_criteria>
    `aptus start` corre end-to-end sobre el pack real: sesión select navegable (volver atrás cambia respuesta), Ctrl+C limpio, y resultado por dimensión con N sin agregado. El smoke no interactivo pasa y verifica la integración motor+render de forma determinista. `tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>`aptus start` corre end-to-end (sesión select navegable, Ctrl+C limpio, resultado por dimensión con N); smoke no interactivo en verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Entrada de terminal + ruta del pack → CLI | El usuario interactúa por terminal; la ruta del pack por defecto es una constante bajo `packs/`. |
| Pack YAML real → `startCommand` (vía loadPack) | Única entrada de datos externa; validada fail-fast antes de arrancar la sesión. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Denial of Service | `startCommand` con pack real malformado | low | mitigate | `loadPack` valida fail-fast; `startCommand` captura el error, imprime mensaje claro y sale con código ≠ 0 (nunca stack trace crudo ni sesión sobre datos rotos). |
| T-01-06 | Denial of Service | Ctrl+C a mitad de sesión | low | mitigate | Captura de `ExitPromptError` en `runSession` → salida limpia sin corromper estado (no hay persistencia en P1). |
| T-01-02 | Tampering | Ruta del pack (traversal si en el futuro se aceptara ruta arbitraria del usuario) | low | mitigate | En P1 la ruta del pack es una constante fija bajo `packs/`, no entrada del usuario; no hay superficie de path traversal. Se documenta para revisar cuando se añadan packs seleccionables. |
</threat_model>

<verification>
- `npx vitest run src/cli/render.test.ts src/cli/start.integration.test.ts` en verde.
- Grep gates: sin agregado en render/start; sin `Math.random`/`Date.now` en el núcleo.
- `npx tsc --noEmit` sin errores.
- UAT manual (human-check): sesión select navegable end-to-end sobre el pack real, Ctrl+C limpio, resultado por dimensión con N.
</verification>

<success_criteria>
El walking skeleton está completo: `aptus start` carga el pack real, corre una sesión select equilibrada y navegable, puntúa con el motor puro y renderiza el resultado por dimensión con N — sin ningún score agregado. Motor, contenido y sesión funcionan juntos de punta a punta.
</success_criteria>

<output>
Crear `.planning/phases/01-fundamento-end-to-end/01-05-SUMMARY.md` al terminar.
</output>
