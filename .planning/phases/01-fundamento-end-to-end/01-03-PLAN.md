---
phase: 01-fundamento-end-to-end
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/core/scoring.ts
  - src/core/scoring.test.ts
  - src/core/session.ts
  - src/core/session.test.ts
autonomous: true
requirements: [ENG-01, ENG-02, ENG-04, SESS-01, SESS-02]
must_haves:
  truths:
    - "`score(answered, bank)` es una función PURA: sin I/O, sin terminal, sin `Date.now()`, sin `Math.random()` — determinista y testeable con fixtures (ENG-01)."
    - "`ScoreResult` desglosa por dimensión (y por subtema) con la forma `{ presented, answered, correct, pct }`: el N (`answered` y `presented`) acompaña SIEMPRE al score (RES-01, ENG-02)."
    - "Las preguntas no respondidas cuentan en `presented` pero NO en `answered`; `pct = correct/answered` y vale 0 (nunca NaN) cuando `answered === 0`."
    - "Invariantes siempre ciertas: `answered <= presented` y `pct ∈ [0,1]`; ninguna pregunta suma a `correct` sin sumar antes a `answered`."
    - "`selectBalanced(bank, target, minPerDimension, shuffle)` reparte por dimensión respetando el mínimo cuando el pool lo permite, es determinista con la misma seed, y NO lanza si el pool es menor que el mínimo (toma el máximo disponible) (SESS-02)."
    - "La navegación de la sesión (avanzar, volver atrás, registrar/cambiar respuesta) es lógica PURA en `src/core/session.ts`, testeable sin terminal (base de SESS-01)."
  prohibitions:
    - "`ScoreResult` NO contiene ningún campo agregado tipo `overall`/`total`/`employability`/`hireProbability`: nunca un score único agregado."
    - "El scoring NO da crédito parcial: la corrección es binaria por pregunta."
    - "`src/core/**` NO contiene ningún literal del vocabulario de dominio del pack; opera únicamente sobre las dimensiones que declara el pack en tiempo de ejecución (ENG-04)."
    - "`src/core/**` NO usa `Math.random()` ni `Date.now()`; la aleatoriedad se inyecta como parámetro `ShuffleFn`."
  artifacts:
    - path: "src/core/scoring.ts"
      provides: "Función pura score() + tipos AnsweredQuestion, DimensionScore, ScoreResult (sin campo agregado)"
      exports: ["score"]
    - path: "src/core/session.ts"
      provides: "selectBalanced() (selección equilibrada, shuffle inyectable) + estado de sesión navegable puro (buildSession + transiciones avanzar/volver/responder)"
      exports: ["selectBalanced"]
    - path: "src/core/scoring.test.ts"
      provides: "Cobertura de invariantes de scoring (N, pct∈[0,1], no NaN, binario, sin agregado, multi-respuesta)"
    - path: "src/core/session.test.ts"
      provides: "Determinismo con seed, mínimo por dimensión, pool pequeño sin excepción, navegación"
  key_links:
    - from: "src/core/session.ts"
      to: "src/core/random.ts (plan 01)"
      via: "el shuffle (ShuffleFn) se inyecta desde makeSeededShuffle en los tests; el núcleo no importa aleatoriedad global"
      pattern: "ShuffleFn"
    - from: "src/core/scoring.ts"
      to: "src/content/schema.ts (plan 01)"
      via: "import del tipo Question (solo tipo, sin acoplamiento de runtime)"
      pattern: "import type .*Question"
---

<objective>
El corazón honesto del sistema: el motor de scoring puro (`score`) y el motor de sesión puro (`selectBalanced` + estado navegable). Ambos son funciones deterministas sin I/O, testeadas con fixtures vitest. Aquí se materializan los quality gates de honestidad del proyecto (ENG-01/02/04, SESS-02, RES-01) y la lógica de navegación que el runner interactivo (plan 05) solo tiene que envolver.

Purpose: El scoring es la promesa central de Aptus: nunca miente, nunca agrega en un score único, y el N va siempre junto a cada porcentaje. Al ser puro y determinista, se prueba exhaustivamente con fixtures antes de que ninguna capa de terminal lo toque (recomendación primaria del research: construir el motor con fixtures primero, conectar el runner al final).

Output: `src/core/scoring.ts`, `src/core/session.ts` y sus tests, cubriendo los casos felices y todas las invariantes y casos borde del research.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-fundamento-end-to-end/01-RESEARCH.md
@src/content/schema.ts
@src/core/random.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Motor de scoring puro (score) con N siempre visible y sin agregado</name>
  <files>src/core/scoring.ts, src/core/scoring.test.ts</files>
  <read_first>
    @.planning/phases/01-fundamento-end-to-end/01-RESEARCH.md — Pattern 2 "Motor de scoring puro" (líneas 297-350) y las invariantes de "Validation Architecture" (líneas 683-688).
    @src/content/schema.ts — tipo Question.
    @test/fixtures/mini-pack/ — fixtures válidas para construir los casos de test.
  </read_first>
  <behavior>
    - Todas las preguntas respondidas correctamente en una dimensión → `pct === 1`, `correct === answered === presented`.
    - Una dimensión con algunas no respondidas → `answered < presented`, y `pct = correct/answered` (no sobre presented).
    - Dimensión con 0 respondidas → `answered === 0` y `pct === 0` (NO NaN, NO división por cero).
    - Respuesta incorrecta no suma a `correct` pero sí a `answered` (corrección binaria, sin crédito parcial).
    - Desglose por subtema dentro de la dimensión coherente con el desglose por dimensión.
    - Una pregunta con `correct` como array (multi-respuesta futura) puntúa como acierto solo si la opción elegida está en el array.
    - `ScoreResult` no expone ningún campo agregado global.
  </behavior>
  <action>
    Implementar `src/core/scoring.ts` copiando el Pattern 2 del research: tipos `AnsweredQuestion` (questionId, selectedOptionId nullable), `DimensionScore` (dimension, presented, answered, correct, pct) y `ScoreResult` (solo `byDimension`, y el desglose por subtema; SIN ningún campo agregado). `score(answered, presentedBank)` recorre el banco presentado agrupando por dimensión (y subtema), cuenta presented/answered/correct con corrección binaria, y calcula `pct = answered>0 ? correct/answered : 0`.
    - Cero imports de `src/cli/`; cero `Date.now()`/`Math.random()`.
    - Importar el tipo `Question` desde el schema SOLO como tipo (`import type`).
    - `scoring.test.ts`: cubrir el behavior de arriba con fixtures deterministas (armadas a mano o derivadas del mini-pack), afirmando explícitamente cada invariante (incluido el caso de 0 respondidas sin NaN y la ausencia de campo agregado).
    - No escribir en los ficheros de `src/core/` ningún comentario que nombre ejemplos de dominios concretos; describir todo en términos de "dimensión"/"subtema" genéricos.
  </action>
  <verify>
    <automated>npx vitest run src/core/scoring.test.ts</automated>
    <automated>test "$(grep -riE 'overall|employab|hire.?prob|total.?score' src/core/scoring.ts | wc -l)" -eq 0</automated>
    <automated>test "$(grep -riE 'Date\.now|Math\.random' src/core/scoring.ts | wc -l)" -eq 0</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `score()` es puro y determinista; `ScoreResult` desglosa por dimensión y subtema con presented/answered/correct/pct, sin campo agregado; `pct` nunca es NaN; corrección binaria. Todos los tests de invariantes pasan.
  </acceptance_criteria>
  <done>score() puro, sin agregado, N siempre presente, pct nunca NaN, binario; tests de invariantes en verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Motor de sesión puro (selección equilibrada + estado navegable)</name>
  <files>src/core/session.ts, src/core/session.test.ts</files>
  <read_first>
    @.planning/phases/01-fundamento-end-to-end/01-RESEARCH.md — Pattern 3 "Selección equilibrada" (líneas 358-390), su "Nota — muestra insuficiente" (línea 392), el diagrama de arquitectura (`buildSession → estado navegable`, líneas 156-159) y el ejemplo de test determinista (líneas 566-586).
    @src/core/random.ts — makeSeededShuffle (ShuffleFn).
  </read_first>
  <behavior>
    - `selectBalanced(bank, target, minPerDimension, shuffle)` con la misma seed produce EXACTAMENTE la misma selección y el mismo orden (determinista).
    - El resultado tiene, por dimensión, al menos `minPerDimension` preguntas cuando el pool de esa dimensión lo permite.
    - Si el pool de una dimensión es menor que `minPerDimension`, NO lanza: toma todo lo disponible de esa dimensión.
    - El estado de sesión navegable permite: registrar una respuesta en el índice actual, avanzar, volver al índice anterior, y cambiar una respuesta ya dada; volver desde el índice 0 no rompe.
    - Al completar, se puede derivar `AnsweredQuestion[]` (preguntas presentadas, con la opción elegida o null si se dejó sin responder).
  </behavior>
  <action>
    Implementar `src/core/session.ts`:
    - `selectBalanced` copiando el Pattern 3 (reparto `Math.max(minPerDimension, floor(target/nDimensiones))`, `take = Math.min(perDim, pool.length)`, shuffle inyectado al elegir y al ordenar; helper `groupBy`). `ShuffleFn` como tipo inyectable — nunca `Math.random()` dentro del núcleo.
    - El estado de sesión navegable como lógica PURA (según el diagrama del research: `buildSession(selected) → { index, answers }` + transiciones puras `answerCurrent`, `goForward`/`goBack` que devuelven un nuevo estado sin mutar el terminal). Esto extrae la lógica de navegación de SESS-01 para poder testearla sin stdin; el runner (plan 05) solo la envuelve con prompts.
    - `session.test.ts`: determinismo con `makeSeededShuffle(42)` (misma seed → mismo resultado, como el ejemplo del research); mínimo por dimensión respetado cuando el pool lo permite; pool pequeño sin excepción (Pitfall 3); y las transiciones de navegación (avanzar, volver, cambiar respuesta, volver desde 0).
    - No nombrar dominios concretos en comentarios; usar "dimensión" genérica.
  </action>
  <verify>
    <automated>npx vitest run src/core/session.test.ts</automated>
    <automated>test "$(grep -riE 'Math\.random' src/core/session.ts | wc -l)" -eq 0</automated>
    <automated>test "$(grep -riE 'embeddings|hallucination|overfitting|regularization|bias-variance|cross-validation' src/core | wc -l)" -eq 0</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `selectBalanced` es puro, determinista con seed y respeta el mínimo por dimensión sin lanzar cuando el pool es pequeño. El estado de sesión navegable es lógica pura testeada. El grep confirma que `src/core/**` no contiene vocabulario de dominio del pack (ENG-04) ni `Math.random()`.
  </acceptance_criteria>
  <done>selectBalanced determinista con mínimo por dimensión (sin lanzar en pool pequeño) + navegación pura testeada; grep ENG-04 en 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguna nueva) | El núcleo es lógica pura sin entrada externa directa; recibe `Question[]` ya validado por el loader (plan 02). No hay frontera de confianza nueva en esta capa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | Integridad del scoring (que un cambio futuro introduzca un agregado, crédito parcial o `Math.random()` en el núcleo) | low | mitigate | Gates automatizados en `<verify>`: grep de ausencia de campos agregados, de `Date.now`/`Math.random` en el núcleo, y de vocabulario de dominio; más los tests de invariantes (pct∈[0,1], no NaN, binario). Estos gates fijan el contrato de honestidad en CI. |
| T-01-05 | Denial of Service | `selectBalanced` con `minPerDimension` mayor que el pool | low | accept | Por diseño (Pattern 3) toma el máximo disponible sin lanzar; el aviso de "muestra insuficiente" es responsabilidad del loader/composición, no del núcleo. Riesgo residual aceptado (una sesión sale con menos preguntas de las pedidas, nunca un crash). |
</threat_model>

<verification>
- `npx vitest run src/core/scoring.test.ts src/core/session.test.ts` en verde.
- Grep gates: sin campo agregado en `ScoreResult`; sin `Date.now`/`Math.random` en el núcleo; sin vocabulario de dominio en `src/core/**` (ENG-04).
- `npx tsc --noEmit` sin errores.
</verification>

<success_criteria>
El motor de scoring y el de sesión son puros, deterministas y honestos: desglose por dimensión con N, sin agregado, sin crédito parcial, agnósticos del dominio, con aleatoriedad inyectable y navegación testeable. Listos para que el runner (plan 05) los envuelva.
</success_criteria>

<output>
Crear `.planning/phases/01-fundamento-end-to-end/01-03-SUMMARY.md` al terminar.
</output>
