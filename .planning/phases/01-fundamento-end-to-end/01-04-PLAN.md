---
phase: 01-fundamento-end-to-end
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - packs/ai-ml-readiness/pack.yaml
  - packs/ai-ml-readiness/questions.yaml
  - packs/ai-ml-readiness/pack.test.ts
autonomous: true
requirements: [CONT-01, CONT-02]
must_haves:
  truths:
    - "Existe un pack REAL en `packs/ai-ml-readiness/` con exactamente 2 dimensiones: 'LLM/RAG/evals' y 'ML clásico' (CONT-01)."
    - "Cada dimensión tiene 12-15 preguntas reales (≥24-30 en total), con una respuesta correcta VERDADERA y una explicación real por pregunta (CONT-02)."
    - "Cada pregunta declara id, dimension, subtopic, difficulty, roles, stem, options (id+text), correct (referencia una option.id existente), explanation, source, date."
    - "El pack real pasa el validador zod real (`loadPack`) sin errores."
    - "El banco tiene ≥ suficientes preguntas por dimensión para satisfacer el mínimo por dimensión de una sesión de ≥15 min (≈18-22 preguntas seleccionadas)."
  prohibitions:
    - "NINGUNA pregunta usa texto de relleno/lorem/placeholder, respuestas inventadas ni explicaciones vacías: todo es contenido técnicamente correcto y curado a mano."
    - "El código de `src/core/**` y `src/content/**` sigue sin conocer estos nombres de dominio: el dominio vive SOLO en este pack (ENG-04)."
  artifacts:
    - path: "packs/ai-ml-readiness/pack.yaml"
      provides: "Metadata del pack: name, version, dimensions [LLM/RAG/evals, ML clásico]"
    - path: "packs/ai-ml-readiness/questions.yaml"
      provides: "Banco real ≥24-30 preguntas de opción múltiple con respuesta correcta y explicación reales"
    - path: "packs/ai-ml-readiness/pack.test.ts"
      provides: "Test que valida el pack real vía loadPack + comprueba nº de dimensiones y cuenta mínima por dimensión"
  key_links:
    - from: "packs/ai-ml-readiness/pack.test.ts"
      to: "src/content/loader.ts (plan 02)"
      via: "carga el pack real con loadPack y afirma que valida"
      pattern: "loadPack\\("
---

<objective>
El pack de contenido real mínimo pero genuino: `packs/ai-ml-readiness/` con 2 dimensiones (LLM/RAG/evals y ML clásico) y ≥24-30 preguntas de opción múltiple técnicamente correctas, cada una con respuesta verdadera y explicación real. Es "contenido, no simulado" (Success Criterion 1 de la fase) y valida de verdad, contra el loader real, que el formato del pack funciona.

Purpose: Sin un banco real, el slice end-to-end sería una demo hueca. Este pack demuestra que un no-programador (Carlos) puede editar contenido a mano en YAML, que el validador lo acepta, y que hay suficiente material para una sesión de ≥15 min repartida por dimensión. El mining serio de `bootcamp-ml-llm` es Phase 2; aquí el banco es hand-curado (source = "curación manual").

Output: `pack.yaml`, `questions.yaml` (banco real) y `pack.test.ts` que valida el pack real con `loadPack`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-fundamento-end-to-end/01-CONTEXT.md
@.planning/phases/01-fundamento-end-to-end/01-RESEARCH.md
@src/content/schema.ts
@src/content/loader.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Metadata del pack + dimensión 1 (LLM/RAG/evals) con 12-15 preguntas reales</name>
  <files>packs/ai-ml-readiness/pack.yaml, packs/ai-ml-readiness/questions.yaml</files>
  <read_first>
    @.planning/phases/01-fundamento-end-to-end/01-CONTEXT.md — "Formato y schema del pack" (líneas 42-48).
    @src/content/schema.ts — la forma exacta que debe cumplir cada pregunta.
  </read_first>
  <action>
    Crear `pack.yaml` con name (p.ej. "AI/ML Readiness"), version "1.0.0" y `dimensions` con los dos identificadores de dimensión (uno para LLM/RAG/evals, otro para ML clásico — usar identificadores estables y legibles, coherentes con el valor `dimension` de cada pregunta).
    Escribir en `questions.yaml` la dimensión 1 (LLM/RAG/evals) con 12-15 preguntas REALES de opción múltiple, técnicamente correctas, cubriendo subtemas como: chunking, embeddings, recuperación (retrieval), alucinación, métricas de evaluación (faithfulness, context-precision), temperatura, y similares. Cada pregunta con: id único, dimension (id de la dimensión 1), subtopic, difficulty (easy|medium|hard), roles[] (p.ej. AI Engineer, LLM Engineer), stem (enunciado claro), options[] (cada una id + text; ≥2, típicamente 4), correct (id de la opción correcta, que DEBE existir en options), explanation (por qué la correcta es correcta — real, no relleno), source: "curación manual", date (ISO).
    - Cada respuesta correcta debe ser genuinamente correcta y cada explicación real. Nada de lorem/placeholder.
    - Repartir difficulty (algunas easy, varias medium, algunas hard) para dejar preparado el etiquetado que Phase 4 usará en el readiness por rol.
  </action>
  <verify>
    <automated>node -e "const YAML=require('yaml'); const fs=require('fs'); const q=YAML.parse(fs.readFileSync('packs/ai-ml-readiness/questions.yaml','utf8')); const arr=Array.isArray(q)?q:q.questions; const d1=arr.filter(x=>x.dimension===YAML.parse(fs.readFileSync('packs/ai-ml-readiness/pack.yaml','utf8')).dimensions[0]); if(d1.length<12) throw new Error('dimensión 1 necesita >=12 preguntas, tiene '+d1.length)"</automated>
    <automated>node -e "const YAML=require('yaml'); const fs=require('fs'); const q=YAML.parse(fs.readFileSync('packs/ai-ml-readiness/questions.yaml','utf8')); const arr=Array.isArray(q)?q:q.questions; for(const x of arr){const ids=new Set(x.options.map(o=>o.id)); if(!ids.has(x.correct)) throw new Error('correct inexistente en '+x.id); if(!x.explanation||x.explanation.length<15) throw new Error('explicación de relleno en '+x.id)}"</automated>
  </verify>
  <acceptance_criteria>
    `pack.yaml` declara 2 dimensiones. `questions.yaml` contiene ≥12 preguntas reales de la dimensión 1, cada una con `correct` válido y explicación real (≥15 chars). Sin relleno.
  </acceptance_criteria>
  <done>pack.yaml con 2 dimensiones + ≥12 preguntas reales de LLM/RAG/evals con correct válido y explicación real.</done>
</task>

<task type="auto">
  <name>Task 2: Dimensión 2 (ML clásico) con 12-15 preguntas reales + test de validación del pack real</name>
  <files>packs/ai-ml-readiness/questions.yaml, packs/ai-ml-readiness/pack.test.ts</files>
  <read_first>
    @src/content/loader.ts — loadPack, para el test.
    @src/content/schema.ts — forma de la pregunta.
  </read_first>
  <action>
    Añadir a `questions.yaml` la dimensión 2 (ML clásico) con 12-15 preguntas REALES de opción múltiple, técnicamente correctas, cubriendo subtemas como: bias-variance, overfitting, regularización, precision/recall/F1, validación cruzada, descenso de gradiente, escalado de features, y similares. Mismo esquema completo por pregunta que la dimensión 1 (id, dimension = id de la dimensión 2, subtopic, difficulty, roles, stem, options, correct válido, explanation real, source, date).
    Escribir `packs/ai-ml-readiness/pack.test.ts` que: cargue el pack real con `loadPack` (debe validar sin lanzar); afirme que hay exactamente 2 dimensiones; afirme que cada dimensión tiene ≥12 preguntas (suficiente para el mínimo por dimensión de una sesión de ≥15 min); y afirme que el total es ≥24.
  </action>
  <verify>
    <automated>npx vitest run packs/ai-ml-readiness/pack.test.ts</automated>
    <automated>node -e "const YAML=require('yaml'); const fs=require('fs'); const q=YAML.parse(fs.readFileSync('packs/ai-ml-readiness/questions.yaml','utf8')); const arr=Array.isArray(q)?q:q.questions; if(arr.length<24) throw new Error('banco total <24: '+arr.length)"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    El pack real tiene 2 dimensiones con ≥12 preguntas cada una (≥24 total), pasa `loadPack` sin errores y su `pack.test.ts` está en verde. Todas las preguntas de la dimensión 2 son reales con `correct` válido y explicación real.
  </acceptance_criteria>
  <done>Banco real ≥24 preguntas (2 dimensiones) que pasa loadPack; pack.test.ts en verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pack YAML editado a mano → `loadPack` | El pack real es contenido curado a mano; el validador lo trata como no confiable (mismo control que el mini-pack). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Denial of Service | Pack real con YAML malformado tras una edición manual | low | mitigate | `pack.test.ts` valida el pack real vía `loadPack` en CI; un error de edición se detecta antes de ejecutar una sesión. |
| T-01-02 | Tampering | Integridad de contenido (respuesta marcada como correcta que en realidad no lo es, o `correct` apuntando a una option inexistente) | medium | mitigate | `superRefine` (plan 01/02) garantiza que `correct ∈ options[].id`; la corrección técnica de cada respuesta es responsabilidad de la curación manual y queda auditable vía `source`/`explanation`. Sesgo de autocuración (curador = evaluado) se aborda en Phase 2/4 (fuentes externas); fuera de alcance aquí. |
</threat_model>

<verification>
- `npx vitest run packs/ai-ml-readiness/pack.test.ts` en verde (pack real valida vía loadPack, 2 dimensiones, ≥12/dimensión, ≥24 total).
- Checks de contenido: `correct` válido y explicación real (≥15 chars) en todas las preguntas.
- `npx tsc --noEmit` sin errores.
</verification>

<success_criteria>
Existe un pack real, hand-curado, técnicamente correcto, con 2 dimensiones y ≥24-30 preguntas, que pasa el validador real y da material suficiente para una sesión equilibrada de ≥15 min. El dominio vive solo en el pack; el motor sigue sin conocerlo.
</success_criteria>

<output>
Crear `.planning/phases/01-fundamento-end-to-end/01-04-SUMMARY.md` al terminar.
</output>
