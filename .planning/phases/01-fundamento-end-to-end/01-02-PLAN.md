---
phase: 01-fundamento-end-to-end
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/content/loader.ts
  - src/content/loader.test.ts
  - test/fixtures/mini-pack/pack.yaml
  - test/fixtures/mini-pack/questions.yaml
  - test/fixtures/invalid-packs/correct-sin-referencia.yaml
  - test/fixtures/invalid-packs/menos-de-dos-opciones.yaml
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03]
must_haves:
  truths:
    - "`loadPack()` carga un pack YAML válido desde ficheros y devuelve datos tipados (Question[] + metadata) — el contenido vive fuera del código (CONT-01)."
    - "Un pack cargado tiene toda la metadata obligatoria por pregunta: dimension, difficulty, stem, options (≥2), correct, explanation (CONT-02)."
    - "El validador RECHAZA un pack cuyo `correct` no referencia ningún `option.id` existente (CONT-03)."
    - "El validador RECHAZA un pack con una pregunta de menos de 2 `options` (CONT-03)."
    - "Un pack inválido aborta la carga con un `Error` de mensaje claro (fichero + path del issue + motivo), nunca con un stack trace crudo ni datos parciales."
  prohibitions:
    - "NO hay try/catch silencioso alrededor de `safeParse`: se usa el resultado `success`/`error` explícito y se lanza si falla (nunca continuar con datos parcialmente válidos)."
    - "Las fixtures de test usan nombres de dimensión GENÉRICOS (p.ej. 'dimension-alpha'), NO nombres de dominio real — el dominio real solo vive en `packs/`."
  artifacts:
    - path: "src/content/loader.ts"
      provides: "loadPack(): YAML.parse + PackSchema.safeParse + error estructurado + fail-fast"
      exports: ["loadPack"]
    - path: "src/content/loader.test.ts"
      provides: "Cobertura de carga válida + rechazo de packs inválidos"
    - path: "test/fixtures/mini-pack/questions.yaml"
      provides: "Pack válido pequeño y determinista para tests (2 dimensiones genéricas)"
    - path: "test/fixtures/invalid-packs/correct-sin-referencia.yaml"
      provides: "Fixture inválida: correct no referencia ninguna option.id"
    - path: "test/fixtures/invalid-packs/menos-de-dos-opciones.yaml"
      provides: "Fixture inválida: pregunta con <2 options"
  key_links:
    - from: "src/content/loader.ts"
      to: "src/content/schema.ts (plan 01)"
      via: "import de PackSchema y uso de safeParse"
      pattern: "PackSchema\\.safeParse"
---

<objective>
El cargador/validador de contenido: `loadPack()` lee el YAML del pack, lo valida con zod (`PackSchema.safeParse` + el `superRefine` que verifica `correct ∈ options[].id`) y falla rápido con mensaje claro si algo no cuadra. Es la única frontera de entrada de datos no confiables del sistema (CONT-01/02/03).

Purpose: Garantizar que ninguna sesión pueda arrancar sobre un pack roto (falta metadata, respuesta correcta inexistente, menos de 2 opciones). El requisito es rechazar antes de usar, no degradar con datos parciales. Esta pieza también crea las fixtures deterministas que consumen los tests del motor puro (plan 03).

Output: `src/content/loader.ts`, su test exhaustivo, una fixture de pack válido (`mini-pack`) y dos fixtures inválidas.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-fundamento-end-to-end/01-RESEARCH.md
@src/content/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixtures de test (mini-pack válido + dos packs inválidos)</name>
  <files>test/fixtures/mini-pack/pack.yaml, test/fixtures/mini-pack/questions.yaml, test/fixtures/invalid-packs/correct-sin-referencia.yaml, test/fixtures/invalid-packs/menos-de-dos-opciones.yaml</files>
  <read_first>
    @.planning/phases/01-fundamento-end-to-end/01-RESEARCH.md — "Wave 0 Gaps" (líneas 676-681) y "Nota sobre invariantes" (líneas 683-688).
    @src/content/schema.ts — la forma exacta que deben cumplir las fixtures.
  </read_first>
  <action>
    Crear las fixtures deterministas separadas del pack real de `packs/`.
    - `test/fixtures/mini-pack/pack.yaml` + `questions.yaml`: pack VÁLIDO pequeño con 2 dimensiones de nombre GENÉRICO (p.ej. `dimension-alpha`, `dimension-beta`), ~6-8 preguntas por dimensión, cada una cumpliendo el schema completo (id, dimension, subtopic, difficulty, roles, stem, options con id+text, correct que referencia una option.id existente, explanation, source='fixture', date). Contenido de relleno es aceptable AQUÍ (es una fixture de test, no el pack real). Usar nombres genéricos refuerza que el motor es agnóstico del dominio (ENG-04).
    - `test/fixtures/invalid-packs/correct-sin-referencia.yaml`: al menos una pregunta cuyo `correct` apunta a una option.id que NO existe entre sus `options`.
    - `test/fixtures/invalid-packs/menos-de-dos-opciones.yaml`: al menos una pregunta con una sola `option`.
    - Incluir en el mini-pack al menos una pregunta con subtopic para poder verificar el desglose por subtema del scoring (plan 03).
  </action>
  <verify>
    <automated>test -f test/fixtures/mini-pack/questions.yaml && test -f test/fixtures/invalid-packs/correct-sin-referencia.yaml && test -f test/fixtures/invalid-packs/menos-de-dos-opciones.yaml</automated>
    <automated>node -e "const YAML=require('yaml'); const fs=require('fs'); const q=YAML.parse(fs.readFileSync('test/fixtures/mini-pack/questions.yaml','utf8')); const arr=Array.isArray(q)?q:q.questions; if(!arr||arr.length<12) throw new Error('mini-pack debe tener >=12 preguntas')"</automated>
  </verify>
  <acceptance_criteria>
    Existe un mini-pack válido con 2 dimensiones genéricas y ≥12 preguntas, más dos fixtures inválidas (correct sin referencia; <2 opciones). Ningún nombre de dimensión de dominio real aparece en las fixtures.
  </acceptance_criteria>
  <done>Mini-pack válido (2 dimensiones genéricas, ≥12 preguntas) + dos fixtures inválidas creadas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: loader.ts (carga + validación fail-fast) y su test exhaustivo</name>
  <files>src/content/loader.ts, src/content/loader.test.ts</files>
  <read_first>
    @.planning/phases/01-fundamento-end-to-end/01-RESEARCH.md — Pattern 1 loader (líneas 267-289), "Nota sobre errores de sintaxis YAML" (línea 289) y "Anti-Patterns" (líneas 490-493).
    @src/content/schema.ts — PackSchema.
  </read_first>
  <behavior>
    - `loadPack` sobre el mini-pack válido devuelve datos tipados con todas las preguntas y sus metadatos.
    - `loadPack` sobre `correct-sin-referencia.yaml` lanza un Error cuyo mensaje menciona `correct` y el id de la pregunta afectada.
    - `loadPack` sobre `menos-de-dos-opciones.yaml` lanza un Error que indica el fallo de `options`.
    - `loadPack` sobre un pack al que le falta `explanation` en una pregunta lanza un Error (metadata obligatoria).
    - El mensaje de error es legible (fichero + path del issue + motivo), no un stack trace crudo.
  </behavior>
  <action>
    Implementar `loadPack(pathToPackYaml, pathToQuestionsYaml)` copiando el Pattern 1 del research: leer ambos ficheros, `YAML.parse`, mezclar metadata del pack con las preguntas, `PackSchema.safeParse`, y si `!result.success` construir un mensaje agregando `issue.path` + `issue.message` por cada issue y lanzar `Error`. Dejar propagar (o envolver homogéneamente) el error de sintaxis de `YAML.parse`. Devolver `result.data` tipado.
    - Prohibido el try/catch silencioso alrededor de `safeParse` (usar success/error explícito).
    - El loader NO debe contener strings de dominio; opera sobre lo que declare el pack.
    - `loader.test.ts`: cubrir el comportamiento descrito arriba usando las fixtures del Task 1 (carga válida + los rechazos). Afirmar sobre el CONTENIDO del mensaje de error (que menciona la pregunta/campo), no solo que lanza.
  </action>
  <verify>
    <automated>npx vitest run src/content/loader.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `loadPack` carga el mini-pack válido tipado y rechaza (con mensaje claro que identifica pregunta y campo) los tres packs inválidos: correct sin referencia, <2 opciones, y metadata obligatoria ausente. El test pasa; `tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>loadPack carga packs válidos tipados y rechaza los inválidos con mensaje claro; test en verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fichero YAML del pack → `loadPack` | El pack es un fichero editable a mano; el loader lo trata como entrada no confiable (ASVS V5 Input Validation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Denial of Service | `loadPack` (YAML malformado o estructura inesperada, p.ej. `options` como string) | medium | mitigate | Validación completa con zod (`safeParse`) antes de cualquier uso; el error se lanza con mensaje claro para que el CLI (plan 05) salga con código ≠ 0, no con stack trace crudo. |
| T-01-02 | Tampering | `superRefine` (pack con `correct` apuntando a una option.id inexistente → % de fallo inflado sin causa visible) | medium | mitigate | `superRefine` de `QuestionSchema` rechaza el pack completo; no se puede iniciar sesión con datos rotos (CONT-03). Testeado con la fixture `correct-sin-referencia.yaml`. |
| T-01-03 | Tampering | Parser YAML (tags custom que instancian objetos arbitrarios) | low | accept | El paquete `yaml` (eemeli/yaml) usa por defecto el esquema "core" seguro (no ejecuta tags tipo `!!python/object`). No se necesita config extra para packs propios; se acepta el riesgo residual dado que Aptus solo carga packs locales de un único usuario (no red, no multiusuario). |
</threat_model>

<verification>
- `npx vitest run src/content/loader.test.ts` en verde: carga válida + tres rechazos.
- `npx tsc --noEmit` sin errores.
- No hay try/catch silencioso alrededor de `safeParse` (revisión: usa `result.success`).
</verification>

<success_criteria>
El contenido entra al sistema solo por `loadPack`, que devuelve datos tipados para packs válidos y aborta con mensaje claro cualquier pack que incumpla el schema o la referencia de `correct`. Las fixtures deterministas están listas para el motor puro (plan 03).
</success_criteria>

<output>
Crear `.planning/phases/01-fundamento-end-to-end/01-02-SUMMARY.md` al terminar.
</output>
