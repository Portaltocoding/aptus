---
phase: 01-fundamento-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - eslint.config.js
  - .prettierrc
  - .gitignore
  - src/content/schema.ts
  - src/core/random.ts
  - src/core/random.test.ts
autonomous: true
requirements: [ENG-04, CONT-02]
must_haves:
  truths:
    - "El proyecto usa ESM real: `package.json` declara `\"type\": \"module\"` y `npm start`/`tsx` no fallan con error de import."
    - "`npx tsc --noEmit` pasa sin errores sobre el andamiaje."
    - "El schema del pack define `dimension` como cadena libre (`z.string()`), sin enum de dominio (ENG-04)."
    - "`QuestionSchema` exige la metadata obligatoria: dimension, difficulty, stem, options (≥2), correct, explanation; y `superRefine` verifica que `correct` referencia una `option.id` existente (CONT-02)."
    - "`mulberry32`/`makeSeededShuffle` son deterministas: la misma seed produce siempre la misma secuencia/orden."
  prohibitions:
    - "El schema NO enumera nombres de dominio de negocio ('LLM','RAG','ML'...): `dimension` es `z.string()`, nunca `z.enum([...])`."
    - "`src/core/random.ts` NO usa `Date.now()` (la aleatoriedad depende solo de la seed inyectada)."
  artifacts:
    - path: "package.json"
      provides: "Scaffold ESM + scripts start/test/typecheck/lint + dependencias del stack"
      contains: "\"type\": \"module\""
    - path: "src/content/schema.ts"
      provides: "Contrato zod del pack: OptionSchema, QuestionSchema (+superRefine), PackSchema; tipo Question inferido"
      exports: ["QuestionSchema", "PackSchema"]
    - path: "src/core/random.ts"
      provides: "PRNG determinista mulberry32 + makeSeededShuffle (Fisher-Yates) — función pura, sin dependencias"
      exports: ["mulberry32", "makeSeededShuffle"]
    - path: "src/core/random.test.ts"
      provides: "Test de determinismo del PRNG"
  key_links:
    - from: "src/content/schema.ts"
      to: "src/content/loader.ts (plan 02) y src/core/scoring.ts+session.ts (plan 03)"
      via: "export de QuestionSchema/PackSchema y del tipo Question inferido"
      pattern: "export (const|type) (QuestionSchema|PackSchema|Question)"
---

<objective>
Andamiaje del proyecto TypeScript/Node (ESM, tsx, vitest) y los dos artefactos fundacionales sin dependencias de código: el contrato zod del pack (`src/content/schema.ts`) y el PRNG determinista (`src/core/random.ts`). Es la primera pieza del walking skeleton: sienta las bases y el contrato que consumen el cargador (plan 02) y el motor puro (plan 03).

Purpose: Fijar la estructura de capas (núcleo puro / contenido / IO) y el schema del pack desde el primer commit, de forma que todo el contenido entre por `packs/` y el motor sea agnóstico del dominio (ENG-04). Un contrato bien definido evita que los ejecutores de los planes siguientes tengan que reconstruir la forma de los datos.

Output: `package.json` (ESM + scripts + deps), `tsconfig.json`, `vitest.config.ts`, configs de lint/format, `src/content/schema.ts` y `src/core/random.ts` con su test de determinismo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-fundamento-end-to-end/01-CONTEXT.md
@.planning/phases/01-fundamento-end-to-end/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Andamiaje ESM (package.json, tsconfig, vitest, lint) e instalación de dependencias</name>
  <files>package.json, tsconfig.json, vitest.config.ts, eslint.config.js, .prettierrc, .gitignore</files>
  <read_first>
    @.planning/phases/01-fundamento-end-to-end/01-RESEARCH.md — sección "Standard Stack" (comando de instalación, líneas 112-116), "Package Legitimacy Audit" (líneas 118-134) y Pitfall 4 (líneas 527-531).
    @.planning/phases/01-fundamento-end-to-end/01-CONTEXT.md — sección "Scaffolding y tooling" (líneas 28-40).
  </read_first>
  <action>
    Crear el andamiaje del proyecto siguiendo las decisiones LOCKED de CONTEXT.md.
    - `package.json` con `"type": "module"` (imprescindible: commander@15 es ESM-only, ver Pitfall 4), `"name": "aptus"`, `"bin"`/scripts: `start` (`tsx src/index.ts start`), `test` (`vitest run`), `typecheck` (`tsc --noEmit`), `lint` (`eslint .`).
    - Instalar dependencias de runtime con las versiones verificadas en RESEARCH.md Standard Stack: yaml, zod, @inquirer/prompts, commander, cli-table3, picocolors. Y de dev: typescript, tsx, vitest, @types/node, eslint + config flat, prettier.
    - Objetivo TypeScript 7 con red de seguridad: si el tooling de lint da fricción con la API inestable de TS7, fijar `typescript@^6.0.3` sin bloquear (documentar la decisión tomada en el SUMMARY). No bloquear el MVP por lint.
    - `tsconfig.json`: `module`/`moduleResolution` acordes a ESM (NodeNext), `strict: true`, `noEmit: true`, `types: ["node"]`, incluir `src` y `test`.
    - `vitest.config.ts`: config mínima (globals opcional; sin watch). Reporter por defecto.
    - `eslint.config.js` (flat) + `.prettierrc` mínimos y NO bloqueantes para el MVP.
    - `.gitignore`: `node_modules`, artefactos de build/coverage.
    - Legitimidad de paquetes: RESEARCH.md ya adjunta la "Package Legitimacy Audit" con todos los paquetes aprobados. `vitest` y `tsx` aparecen como `SUS ("too-new")` pero es un falso positivo documentado (el seam marca la fecha de la última versión, no la antigüedad real del paquete; ambos existen desde 2015/2021 con decenas de millones de descargas/semana y sin scripts postinstall). NO se requiere checkpoint de verificación humana para estos dos; dejar constancia en el SUMMARY de que se revisó y se descartó el riesgo.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.type!=='module') throw new Error('falta type:module'); if(!p.scripts.start||!p.scripts.test||!p.scripts.typecheck) throw new Error('faltan scripts')"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `package.json` declara `"type": "module"` y los scripts start/test/typecheck/lint. Las dependencias del Standard Stack están instaladas. `npx tsc --noEmit` pasa sobre un proyecto vacío/con solo configs. No hay checkpoint de legitimidad (falso positivo de vitest/tsx documentado).
  </acceptance_criteria>
  <done>ESM andamiado (type:module + scripts), stack instalado, `tsc --noEmit` limpio; sin checkpoint de legitimidad.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contrato zod del pack (schema.ts) + PRNG determinista (random.ts)</name>
  <files>src/content/schema.ts, src/core/random.ts, src/core/random.test.ts</files>
  <read_first>
    @.planning/phases/01-fundamento-end-to-end/01-RESEARCH.md — Pattern 1 "Loader YAML + zod" (schema completo, líneas 222-265) y "Code Examples" mulberry32 + makeSeededShuffle (líneas 537-563).
    @.planning/phases/01-fundamento-end-to-end/01-PATTERNS.md — "Capa src/content/" y "Aleatoriedad inyectable" (líneas 41-47, 110-112).
  </read_first>
  <behavior>
    - random: `makeSeededShuffle(42)` aplicado dos veces al mismo array produce EXACTAMENTE el mismo orden; seeds distintas producen (con alta probabilidad) órdenes distintos.
    - random: `mulberry32(seed)()` devuelve siempre un número en [0,1); misma seed → misma secuencia.
    - schema: un objeto pregunta con toda la metadata válida y `correct` que referencia una `option.id` existente pasa `QuestionSchema.safeParse`; si `correct` no existe entre las options, falla con un issue en `path: ["correct"]`.
    - schema: falta de `explanation` o `options` con <2 elementos hace fallar el parse.
  </behavior>
  <action>
    Copiar casi literalmente el código de RESEARCH.md (es greenfield: los bloques del research son la fuente de verdad de implementación, no mera referencia).
    - `src/content/schema.ts`: `OptionSchema` (id, text), `QuestionSchema` con campos id, dimension, subtopic (opcional), difficulty (enum easy|medium|hard), roles (array, default []), stem, options (min 2), correct (union string | string[]), explanation, source, date; y `.superRefine` que añade un issue por cada id de `correct` que no exista en `options`. `PackSchema` con name, version, dimensions (array de strings), questions (min 1). Exportar también el tipo `Question` inferido (`z.infer`).
    - REGLA CLAVE ENG-04: `dimension` es una cadena libre (`z.string()`) — el pack declara sus dimensiones; el schema jamás enumera nombres concretos de dominio.
    - Usar `superRefine` (no `refine`) para poder reportar múltiples issues con path preciso (Pitfall 2 del research).
    - `src/core/random.ts`: `mulberry32(seed)` y `makeSeededShuffle(seed)` (Fisher-Yates) como funciones puras copiadas del research; sin dependencias externas, sin `Date.now()`, sin `Math.random()`.
    - `src/core/random.test.ts`: test de determinismo (misma seed → mismo orden; el shuffle no muta el array de entrada).
  </action>
  <verify>
    <automated>npx vitest run src/core/random.test.ts</automated>
    <automated>test "$(grep -c 'dimension: z.string()' src/content/schema.ts)" -ge 1</automated>
    <automated>test "$(grep -c 'z.enum' src/content/schema.ts)" -le 1</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `schema.ts` exporta QuestionSchema/PackSchema con `dimension` como `z.string()` (el único `z.enum` permitido es el de `difficulty`) y `superRefine` valida `correct ∈ options[].id`. `random.ts` es determinista y su test pasa. `tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>schema.ts con `dimension: z.string()` y `superRefine` de `correct`; random.ts determinista y testeado en verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → máquina local | La instalación de dependencias trae código de terceros al proyecto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install` (yaml, zod, @inquirer/prompts, commander, cli-table3, picocolors, vitest, tsx, typescript) | low | mitigate | RESEARCH.md adjunta "Package Legitimacy Audit" con todos los paquetes verificados en npm registry (antigüedad real, descargas/semana, repo oficial, sin `postinstall`). `vitest`/`tsx` marcados `SUS ("too-new")` son falso positivo documentado (fecha de última versión, no del paquete). No hay `[ASSUMED]`/`[SLOP]` → no se requiere checkpoint humano. |
| T-01-01 | Denial of Service | schema/loader (pack YAML) | low | mitigate | El contrato zod (`QuestionSchema`/`PackSchema` + `superRefine`) definido aquí es la base de la validación fail-fast que rechaza packs malformados en el plan 02, antes de que el núcleo o la CLI vean datos inválidos. |
</threat_model>

<verification>
- `npx tsc --noEmit` sin errores.
- `npx vitest run` en verde (test de determinismo del PRNG).
- `package.json` con `"type": "module"` y scripts start/test/typecheck.
- `schema.ts`: `dimension` es `z.string()` (ENG-04); `superRefine` presente para `correct`.
</verification>

<success_criteria>
El proyecto está andamiado en ESM con el stack instalado; el contrato del pack y el PRNG determinista existen, tipan y testean en verde. Los planes 02 y 03 pueden consumir `schema.ts` y `random.ts` sin reconstruir contratos.
</success_criteria>

<output>
Crear `.planning/phases/01-fundamento-end-to-end/01-01-SUMMARY.md` al terminar.
</output>
