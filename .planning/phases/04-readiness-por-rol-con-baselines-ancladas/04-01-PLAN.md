---
phase: 04-readiness-por-rol-con-baselines-ancladas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packs/ai-ml-readiness/readiness.yaml
  - src/content/readiness.ts
  - src/core/readiness.ts
  - src/cli/render.ts
  - src/cli/commands/start.ts
  - src/core/readiness.test.ts
  - src/content/readiness.test.ts
  - src/cli/render.test.ts
  - src/cli/start.integration.test.ts
autonomous: true
requirements: [ENG-03, CONT-05, RES-02, RES-03]
must_haves:
  truths:
    - "El resultado da readiness por rol (junior/mid/senior-ready) contra baselines ancladas a fuentes externas, con umbrales anclados a la dificultad etiquetada de las preguntas (ENG-03, CONT-05, RES-02)."
    - "NUNCA hay un score único agregado de 'empleabilidad': una fila por rol con la evidencia por dificultad y su N."
    - "El resultado lista gaps priorizados (más débil primero) con un plan de estudio concreto por gap (RES-03)."
    - "Las baselines/perfiles de rol viven en datos (readiness.yaml), validados fail-fast, con su fuente citada; el motor de readiness es puro."
  prohibitions:
    - "El motor no adquiere I/O ni conocimiento del dominio hardcodeado; los perfiles vienen de la config."
    - "No se concede un nivel sin evidencia en el tramo de dificultad que exige (no sobre-afirmar)."
  artifacts:
    - path: "packs/ai-ml-readiness/readiness.yaml"
      provides: "Niveles (umbral por dificultad) + perfiles de rol (core/secondary + source) + plan de estudio por dimensión"
    - path: "src/content/readiness.ts"
      provides: "loadReadiness + zod schema fail-fast"
      exports: ["loadReadiness", "ReadinessConfig"]
    - path: "src/core/readiness.ts"
      provides: "computeReadiness (por rol/nivel) + computeGaps (priorizados con estudio), puro"
      exports: ["computeReadiness", "computeGaps", "RoleReadiness", "Gap", "GAP_THRESHOLD"]
    - path: "src/cli/render.ts"
      provides: "renderReadiness + renderGaps"
      exports: ["renderReadiness", "renderGaps"]
---

<objective>
Resolver el flag de research CONT-05 (anclar a fuentes externas) y construir el readiness por rol con gaps y plan de estudio, sobre el resultado ya existente. Cálculo en motor puro; datos y fuentes en la config del pack; CLI solo presenta.
</objective>

<tasks>
<task type="auto">
  <name>Task 0: research — anclar niveles y perfiles a fuentes externas</name>
  <files>.planning/phases/04-.../04-RESEARCH.md, packs/ai-ml-readiness/readiness.yaml</files>
  <action>Buscar marcos de leveling y descripciones de rol 2026; sintetizar en 04-RESEARCH.md con fuentes y codificar en readiness.yaml (levels + roles con source + study).</action>
</task>

<task type="auto" tdd="true">
  <name>Task 1: núcleo — motor de readiness + loader de config</name>
  <files>src/content/readiness.ts, src/core/readiness.ts, src/core/readiness.test.ts, src/content/readiness.test.ts</files>
  <action>
    - loadReadiness con zod fail-fast.
    - computeReadiness: por rol, acierto por tramo de dificultad sobre dimensiones núcleo; nivel más alto cuyos tramos con exigencia tienen evidencia y superan umbral.
    - computeGaps: dimensiones < 70%, de más débil a menos, con recurso de estudio.
    - Tests: senior/junior/pre-junior, no sobre-afirmar sin evidencia, solo dims núcleo, gaps ordenados y con plan; loader válido/ inválido.
  </action>
  <verify>
    <automated>npx vitest run src/core/readiness.test.ts src/content/readiness.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>test "$(grep -riE 'Math\.random|Date\.now' src/core/readiness.ts | wc -l)" -eq 0</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: CLI — render de readiness y gaps + composición</name>
  <files>src/cli/render.ts, src/cli/commands/start.ts, src/cli/render.test.ts, src/cli/start.integration.test.ts</files>
  <action>renderReadiness (tabla por rol con evidencia por dificultad y N, sin agregado) + renderGaps ("te falta X → haz Z"); start.ts carga readiness.yaml (fail-fast) y compone ambos bajo resultado y calibración.</action>
  <verify>
    <automated>npx vitest run</automated>
    <human-check>Sesión real (PTY): ver readiness por rol y gaps con plan al terminar.</human-check>
  </verify>
</task>
</tasks>

<success_criteria>
Readiness por rol anclado a fuentes externas y a la dificultad, sin score agregado, con gaps priorizados y plan de estudio. Verificado con tsc + vitest + UAT sobre PTY.
</success_criteria>

<output>Crear 04-01-SUMMARY.md al terminar.</output>
