node_modules/
dist/
coverage/
*.log
.DS_Store

# Historial de sesiones (datos personales, Phase 5) — no se versiona.
data/

# Andamiaje de planificación (GSD): útil en local, ruido en el repo público.
.planning/

# `drafts/` NO se ignora a propósito (PACKFLOW-02): un borrador es contenido en
# revisión, y que aparezca en el diff es lo que hace que alguien lo lea.
